1. 确认用户任务的输入和输出类型
  - 直接提及的文件类型（PDF/Excel/图像等），并编写相应的工具函数，将这些文件转成大模型可以接受的字符串文本类型
  - 通过任务动词推断类型（报告->PDF，表格->Excel）
  - 无特殊说明时，输入和输出都是字符串文本

2. Agent 里面 tool 和 知识库

3. 支持批量处理任务，分层架构下，可以批量处理不同类型的任务

输出添加一个 type 字段，代表采用的编排模式